# 6 — The PS-compliance slide

One slide, one table. Left column is the problem statement in its own words. Right column is
the screen that answers it and the number that screen prints for the demo case.

This is the slide to put up when a judge asks *"does it actually do what the PS asked?"* — and
the slide to leave up while you demo, because every row can be pointed at.

**Everything in the right-hand column is a real output of `data/processed/cases/demo.json`,
generated by `.venv/bin/python scripts/run_api.py --build-demo` on scene `00053`.** Nothing
here is aspirational. If you change the pipeline, regenerate the case and re-read this file's
numbers off it — the commands are in §6.6.

---

## 6.1 The slide

Render this as a two-column table. Left column narrow, right column wide. If it must be one
column on a projector, keep the clause and drop the *Evidence* line.

| The problem statement asks | SpillTrace answers |
|---|---|
| **(a)** *"Detect and characterise the oil spill…"* | **Screen 2 · Satellite analysis** — U-Net segmentation of the supplied VV/VH bands. **Pooled scene IoU 0.782**, precision 0.830, recall 0.931 across 36 held-out scenes at threshold 0.8. Like-for-like against a classical dB-contrast baseline at patch scale: **0.771 vs 0.582 IoU, +0.189**. *Evidence: the prediction, the reference mask and the probability field are all viewable side by side.* |
| **(a)** *"…calculating geometric properties…"* | **Screen 3 · Slick analysis** — **9 disconnected regions, 223.19 km² total**; largest region **148.39 km²**, perimeter 105.15 km, length 20.54 km, width 12.43 km, elongation 1.65, orientation 177.8° from north. *Evidence: areas are integrated row-by-row on a sphere of radius 6 371 008.8 m, so a degree of longitude shortens with latitude instead of being a constant.* |
| **(a)** *"…and age if feasible."* | **Screen 4 · Drift reconstruction → Estimated spill age** — **up to 24 h at acquisition** (0 h – 24 h). And the honest part: **not resolvable.** Over the full hindcast the estimated position moves **8.6 km against an 11.3 km P90 uncertainty radius — 0.76 of one radius**, so every release time in the window is equally consistent with one image. The card states the test, the arithmetic, and three data sources that would narrow it. |
| **(b)** *"Using oceanographic **and** meteorological data…"* | **Screen 4 · Forcing** — both, not one. Surface current **0.072 m/s** (curl of a streamfunction over M2, K1 and M4 tidal modes, so analytically non-divergent) **plus wind drift 0.168 m/s** at a 3 % windage factor on a 5.6 m/s wind. Wind drift is the larger term, as it is in reality. **Labelled `Drift forcing: Synthetic scenario data`** — the supplied CMEMS product does not cover this scene at this time, and the screen says so with the reason. |
| **(b)** *"…trace the slick towards the origin point **and time**…"* | **Screen 4 · Estimated origin** — point **and** time. Origin centroid **54.6376 °E, 25.5161 °N**; P50 radius **7.58 km**, P90 radius **11.26 km**; release window **2017-03-10 02:15 UTC → 2017-03-11 02:15 UTC**. 300 particles, RK2 midpoint advection, 48 steps of 30 min, diffusivity 8.0 m²/s, seed 26143 — **the same run reproduces bit-for-bit**. |
| **(b)** *"…predict the future flow of the slick."* | **Screen 4 · Forward** — the same integrator run forward 24 h. 300 particles released, **0 beached, 0 left the domain**. Timeline, spread envelopes (P50/P90/max) and a scrubbable clock. |
| **(c)** *"…reconstruct vessel traffic around the origin window in space and time."* | **Screen 5 · Vessel attribution** — **987 AIS reports, 10 vessels**, emitted in the **exact 17-column MarineCadastre/NAIS schema** the PS names as the format authority. Downloadable at `/api/cases/demo/ais.csv` and diff-able against a real daily extract, header byte-for-byte. |
| **(c)** ***"The irrelevant traffic is to be filtered out"*** | **Screen 5 · Traffic filtering** — the funnel, published as counts that add up: **987 reports · 10 vessels → 9 with reports in the release window → 2 intersect the origin envelope → 8 excluded as irrelevant traffic** (1 never in the window, 7 in the window but too far). The rule is printed beside it. Excluded vessels are **retained and dimmed, not deleted**, so the filter itself can be audited. |
| **(c)** *"…scored considering various aspects such as **proximity, trajectory, behavioural anomalies** etc."* | **Screen 5 · Score breakdown** — all three named aspects and three more, weighted to 100: **proximity 30 · time window 25 · trajectory 20 · behavioural anomaly 10 · vessel type 10 · data quality 5.** Top candidate scores **91.5 / 100**. Every component shows its number *and the sentence that earned it*. |
| **Expected solution** *"An automated … model"* | **One command, nine stages, 19.0 s** on a laptop for a full 2048 × 2048 scene: decode 9.7 s · detect 5.7 s · geometry 2.6 s · forcing 0.03 s · backward 0.27 s · forward 0.25 s · AIS 0.21 s · scoring 0.08 s · previews 0.21 s. Timings are shown on Screen 1, measured on the run that produced the case. |
| **Expected solution** *"…**hindcasting** …"* | Their word, implemented literally: the same integrator runs **backward** to the origin and **forward** to the forecast, sharing one forcing field. |
| **Expected solution** *"…ranks potential culprit vessel based on **spatio-temporal correlation** with AIS data."* | Correlation is spatio-*temporal* by construction: each AIS report is compared against where the oil is estimated to have been **at that report's own timestamp**, in units of the envelope radius at that timestamp — not against a static circle. That is why "present somewhere during a 24-hour window" earns **zero**. |
| **Expected solution** *"A suitable **visual interface** is also to be developed."* | **Six screens**, responsive to 375 px, printable, and **zero JavaScript dependencies** — 324 KB of plain ES modules, no build step. Runs offline from a bundled seeded case. |

---

## 6.2 The two rows that win the slide

Do not let the table read as thirteen equal rows. Two of them are the ones other teams will not
have, and they are the two the PS was most prescriptive about.

**The filtering funnel.** Requirement (c) does not say "score the traffic" — it says
*"the irrelevant traffic is to be filtered out"*. That is an instruction to throw something
away **and be seen to throw it away**. Most submissions will show a ranked list of ten vessels
and call the low scores "filtered". Ours shows the population before the filter, the population
after, the rule, and the two distinct reasons a vessel can be excluded:

> 987 AIS reports · 10 vessels → 9 with reports in the release window → 2 intersect the origin
> envelope (within 3 envelope radii) → 8 excluded as irrelevant traffic

Say this out loud: **"the two exclusion reasons mean different things. Seven vessels were in the
window but 45 to 91 km away — those mean look at a different ship. One passed within 1.5 km, but
outside the window: right place, wrong time. That one isn't excluded by distance, it's excluded by
the width of our own release window, and tightening the window is a forcing-data problem."**

**The age we refused to fake.** Requirement (a) says *"and age if feasible"*. The tempting move
is to print the midpoint of the 24-hour window and call it 12 hours. We measured whether the
hindcast can tell one end of the window from the other, found it cannot, and say so — with the
arithmetic on screen.

> "The age is bounded at 24 hours, and we can show you why it can't be tightened: the estimated
> position moves 8.6 km while the uncertainty around it is 11.3 km. The whole window sits inside
> its own error bar. A second acquisition would fix it, and that is a procurement decision, not
> a modelling one."

To an NTRO audience — a technical intelligence organisation — a stated bound with its
derivation is worth more than a confident number. This row is the one that signals you
understand what you are allowed to claim.

---

## 6.3 The rows a judge will push on

Have the number ready. Do not improvise these.

| Push | Answer |
|---|---|
| "Your AIS is fake." | "Synthetic AIS is permitted in writing by the problem statement — *'Real AIS if available may be used else synthetic data can be prepared for the region of oil spill to demonstrate the functioning of the algorithm.'* The condition is that the **algorithm** must be real, and it is. And we emit the exact 17-column MarineCadastre schema the PS names as the format authority — you can download it from the app and diff it against a real daily extract. Switching to a live feed is a file drop, not a rewrite." |
| "The ocean forcing is synthetic too." | "Yes, and the app labels it on every screen that uses it. The supplied CMEMS product doesn't cover this scene at this time — the interface prints the overlap test and the reason it failed rather than silently substituting. The physics is real: the field is the curl of a streamfunction, so it's analytically non-divergent, which we verify numerically in the test suite. If it weren't, the flow itself would concentrate the slick and every area number downstream would be an artefact of the integrator." |
| "0.78 IoU — how clean is that number?" | **Volunteer this before they ask.** "Splits are grouped by parent acquisition, and we'll be straight with you: we found a wiring bug where that grouping wasn't reaching the splitter. 34 of 36 test scenes share an acquisition with a training scene. Treat every accuracy figure as an upper bound. It's fixed, there's a regression test, and the re-run is pending." See `KNOWN-ISSUES.md` §1. |
| "Can it reject a look-alike — an algal bloom, a low-wind glassy patch?" | "Unmeasured, and we say so on the methodology screen. The dataset contains no labelled look-alikes, so the false-alarm rate on clean sea is unknown. It's a dataset gap, not a code gap — and it's the first thing we'd buy data for." |
| "Which vessel did it?" | "We don't answer that, and that's deliberate. The strongest phrase anywhere in the product is **'Priority candidate for investigation.'** We rank by spatio-temporal correlation with every component and its evidence exposed. A false accusation against a named, identifiable vessel is a diplomatic problem, not a software bug." |
| "Why Persian Gulf data and not Indian waters?" | "Because it's the dataset the problem statement recommended — the Zenodo Sentinel-1 SAR oil spill dataset. The pipeline is region-agnostic; it reads the scene's own corner coordinates." |

---

## 6.4 How to build the actual slide

Keep it to **one** slide. Thirteen rows will not fit legibly on a projector, so:

- **Print the full table as a handout** (this file, §6.1) and put it on the judges' table.
- **On screen, show five rows:** (a) detect, (a) age, (b) origin point *and* time,
  (c) filtering funnel, (c) scoring. Those five are the spine of the PS.
- Set the left column at ~35 % width, italic, in the PS's own words with the key phrase in
  bold. Set the right column at ~65 %, with **the number in bold and the screen name first**.
- Every right-hand cell must name a screen. The point of the slide is that each claim is one
  click away — and during the demo you should actually make that click.
- No logos, no icons, no gradient. A judge reading a compliance table wants density, not
  design.

---

## 6.5 The one-paragraph version

If you get thirty seconds instead of a slide:

> The statement has three requirements. **(a)** We segment the slick and characterise it — 9
> regions, 223 km², pooled scene IoU 0.78 against a 0.58 classical baseline — and we bound its
> age at 24 hours, with the arithmetic showing why one acquisition can't tighten that. **(b)** We
> hindcast to an origin point *and* time — 54.64 °E, 25.52 °N, ±11.3 km, in a 24-hour release
> window — under both currents and wind, then forecast forward. **(c)** We reconstruct AIS traffic
> through that window in the exact MarineCadastre schema, filter it — 10 vessels down to 2, with
> the 8 exclusions retained and reasoned so the filter can be audited — and score what's left on
> proximity, trajectory and behavioural anomaly, every component showing the sentence that
> earned it. All nine stages run in 19 seconds from one command, and the interface has zero
> dependencies. The AIS and the ocean forcing are synthetic, labelled as such on every screen,
> and permitted in writing by the statement.

---

## 6.6 Regenerating the numbers in this file

Every figure above comes from the stored demo case or the metrics file. After any pipeline
change, rebuild and re-read them:

```bash
.venv/bin/python scripts/run_api.py --build-demo && .venv/bin/python scripts/build_web.py
```

Then print the rows this slide depends on:

```bash
.venv/bin/python -c "import json; d=json.load(open('data/processed/cases/demo.json')); s=d['slick']; a=d['spillAge']; f=d['attribution']['filtering']; o=d['drift']['backward']['originEstimate']; print('slick   ', s['slickCount'], 'regions', s['totalAreaKm2'], 'km2 total,', s['areaKm2'], 'km2 largest'); print('age     ', a['label'], '| resolvable:', a['resolution']['resolvable'], '| ratio', a['resolution']['bestSeparation']['ratio']); print('origin  ', o['centroid'], 'P50', o['radiusP50Km'], 'P90', o['radiusP90Km']); print('funnel  ', f['summary']); print('top     ', d['attribution']['candidates'][0]['score'], '/', d['attribution']['candidates'][0]['scoreMax']); print('timing  ', d['timing']['totalSeconds'], 's total')"
```

And the accuracy figures, which come from the model evaluation rather than the case:

```bash
.venv/bin/python -c "import json; m=json.load(open('data/processed/scene_metrics.json')); t=m['test']['atSceneThreshold']; print('scene IoU pooled', round(t['pooled']['iou'],4), '| precision', round(t['pooled']['precision'],4), '| recall', round(t['pooled']['recall'],4), '| scenes', t['scenes'])"
```

---

Previous: **[document 5 — explaining it to judges](05-EXPLAINING-TO-JUDGES.md)**.
Related: **[`KNOWN-ISSUES.md`](../../KNOWN-ISSUES.md)** — read it before quoting any accuracy
number from this table.
