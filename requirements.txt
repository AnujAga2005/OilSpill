# SpillTrace runtime dependencies.
#
# Three packages. Everything else in this project is the Python standard library, including
# the HTTP server, the PNG encoder, the CSV and GeoJSON writers and the .nc reader. The
# frontend has no dependencies at all -- no bundler, no framework, no npm install.
#
#   .venv/bin/python -m pip install -r requirements.txt
#
# Pinned as lower bounds, at the versions this project was built and tested against:
# Python 3.11.15, numpy 2.4.4, opencv-python 4.13.0, pytest 9.0.1.

numpy>=2.0
opencv-python>=4.10

# Tests only.
pytest>=8.0

# Deliberately absent: Pillow (the PNG writer is hand-rolled in
# services/ml/spilltrace_ml/preview.py), scipy and scikit-image (the morphology, connected
# components and contour tracing are in services/ml/spilltrace_ml/geometry.py), shapely (the
# geometry is spherical and lives in the same module), pandas and matplotlib (the CSV writing
# is stdlib csv and every chart is drawn as SVG in the browser).
