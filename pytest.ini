[pytest]
testpaths = tests
pythonpath =
    services/common
    services/ml
    services/drift
    services/api
addopts = -q --strict-markers
filterwarnings =
    error::RuntimeWarning
markers =
    slow: exercises the real dataset or a full training run
